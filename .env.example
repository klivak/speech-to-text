# EchoScribe -- Environment Variables
# Copy this file as .env and fill in the needed values
# WARNING: .env file contains sensitive data, NEVER commit it to git!

# ============================================
# OpenAI API (needed ONLY for API mode)
# ============================================
# Get your key: https://platform.openai.com/api-keys
# Leave empty if using local mode
OPENAI_API_KEY=

# ============================================
# Optional settings
# ============================================
# Path to FFmpeg (if not in system PATH)
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe

# Path to config (default ./config.json)
# CONFIG_PATH=./config.json

# Whisper model cache directory (default ~/.cache/whisper)
# WHISPER_CACHE_DIR=

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log file (leave empty for console-only output)
# LOG_FILE=
